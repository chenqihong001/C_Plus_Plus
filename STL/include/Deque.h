#pragma once
/*双端队列，支持在两段进行高效率的插入删除操作*/
class Deque
{
public:
    // assign()赋值
    // swap()交换
    // resize()
    // push_back(elem);   在容器尾部添加一个数据
    // push_front(elem);  在容器头部插入一个数据
    // pop_back();        删除容器最后一个数据
    // pop_front();       删除容器第一个数据
    // at()
    // operator[]
    // front()
    // back()
    // insert()
    // erase()
    // clear()
};